[project]
name = "pre-commit-hooks"
version = "0.0.1"
description = "Collection of useful pre-commit hooks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.5.1",
]
