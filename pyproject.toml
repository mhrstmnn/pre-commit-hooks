[project]
name = "pre-commit-hooks"
version = "0.0.1"
description = "Collection of useful pre-commit hooks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ty>=0.0.9",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
