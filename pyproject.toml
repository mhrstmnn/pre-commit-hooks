[project]
name = "pre-commit-hooks"
version = "0.0.1"
description = "Collection of useful pre-commit hooks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "clang-format>=22.1.5",
    "clang-tidy>=22.1.0.1",
    "ty>=0.0.34",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
